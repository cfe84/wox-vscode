{
  "name": "vscode-recent",
  "version": "1.0.0",
  "description": "List recent projects in vscode",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "clean": "rm -rf dist/",
    "rebuild": "npm run clean && npm run build",
    "start": "npm run build && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^15.3.0",
    "ncp": "^2.0.0",
    "rimraf": "^3.0.2",
    "ts-loader": "^9.1.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "open": "^8.0.8"
  }
}
